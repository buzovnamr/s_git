# Система синхронизации Telegram - Полная документация

**Обновлено**: 2025-10-21  
**Статус**: Готово к продакшену с кэшированием user_id - **в 3 раза быстрее!** ✅

---

## 🤖 ВАЖНО: Документация для AI агентов

**Эта документация написана ДЛЯ AI АГЕНТОВ, а не для людей.**

**Что это означает**:
- ✅ **Кратко и плотно** - максимум информации, минимум слов
- ✅ **Техническая точность** - точные детали реализации при необходимости
- ✅ **Без подсказок** - агенты могут вывести контекст и заполнить пробелы
- ✅ **Структурировано для парсинга** - четкие секции, искомые паттерны
- ❌ **Без ненужных пояснений** - нет "дружелюбных объяснений" или многословных описаний
- ❌ **Без избыточности** - каждый факт указан один раз, ссылается в других местах при необходимости

**Инструкции для людей**: См. `../../.cursorrules` (родительская папка) для быстрых команд. Это то, что должны читать люди.

**Использование агентом**: Читайте этот файл, когда нужно:
- Понять внутреннюю архитектуру перед изменением скриптов
- Отладить проблемы с системой синхронизации
- Добавить новые функции в существующие скрипты
- Понять форматы данных и структуры файлов

**Стиль написания**: Думайте "техническая спецификация", а не "руководство пользователя". Плотно, точно, искомо.

---

## 🎯 Быстрый старт

### Новый контакт (найти handle + user_id):
```bash
cd Scripts && source venv/bin/activate

# 1. Поиск контакта
python3 telegram_find_handles.py --person "New Contact" --search --query "SearchTerm"
# Возвращает JSON с user_id, например, "user_id": 12345678

# 2. Сохранить с user_id (ОБЯЗАТЕЛЬНО для быстрой синхронизации!)
python3 telegram_find_handles.py --person "New Contact" --save "@username" --user-id 12345678
```

### Пакетное добавление user_ids к существующим контактам:
```bash
cd Scripts && source venv/bin/activate
python3 telegram_add_user_ids_batch.py
# Автоматически добавляет user_ids ко всем контактам
```

### Синхронизировать один контакт (инкрементально):
```bash
cd Scripts && source venv/bin/activate
python3 telegram_sync.py --person "Contact A"
```

### Перезагрузить один контакт с даты:
```bash
cd Scripts && source venv/bin/activate
python3 telegram_sync.py --person "Contact A" --since "2025-10-01"
```

### Синхронизировать все контакты #DO_SYNC:
```bash
cd Scripts && source venv/bin/activate
python3 telegram_sync.py --all --report report.txt
```

### Проверить статус синхронизации:
```bash
cd Scripts && source venv/bin/activate
python3 telegram_analyze_status.py
# Результат → Context/Telegram_Sync_Status.md
```

---

## 📚 Дополнительные файлы документации

1. **[Telegram_Tags_Guide.md](./Telegram_Tags_Guide.md)** - Система тегов (DO_SYNC / DO_NOT_SYNC / NOT_ON_TELEGRAM)
2. **[Telegram_Sync_How_It_Works.md](./Telegram_Sync_How_It_Works.md)** - Техническое глубокое погружение
3. **[Telegram_Quick_Reference.md](./Telegram_Quick_Reference.md)** - Шпаргалка команд
4. **[Telegram_Sync_Status.md](./Telegram_Sync_Status.md)** - Текущий статус контактов (автогенерируется)

---

## 🔄 Три режима синхронизации

### Режим 1: Первая синхронизация (без временной метки)
**Когда**: У контакта нет временной метки `[DONE:...]` в списке синхронизации  
**Что происходит**: Скачивает ВСЕ сообщения из всей истории  
**Файл**: Создает новый файл со всеми сообщениями  

```bash
python3 telegram_sync.py --person "New Contact"
```

**Пример вывода:**
```
📥 Первая синхронизация: скачивание всех сообщений
✅ 115 сообщений
```

---

### Режим 2: Инкрементальная синхронизация (по умолчанию)
**Когда**: У контакта есть временная метка `[DONE:...]`, нет параметра `--since`  
**Что происходит**: Скачивает только НОВЫЕ сообщения после временной метки  
**Файл**: Добавляет новые сообщения в существующий файл  

```bash
python3 telegram_sync.py --person "Contact A"
```

**Пример вывода:**
```
⚡ Инкрементальный режим: с 2025-10-17T18:00:09
✅ 3 новых сообщения (из 118 всего)
```

**File structure:**
```markdown
# Contact A

## Telegram history with Contact A (handle: @contact_a)

[... old messages ...]

## Update from 2025-10-18T10:30:00
### Messages after 2025-10-17T18:00:09

- 2025-10-18T09:15:00 ➡️: New message 1
- 2025-10-18T08:30:00 ⬅️: New message 2
```

---

### Режим 3: Перезагрузка с даты (явный --since)
**Когда**: Вы указываете параметр `--since DATE`  
**Что происходит**: 
- Сохраняет сообщения ДО даты --since
- Удаляет сообщения ПОСЛЕ даты --since
- Перескачивает сообщения с --since и далее

**Случаи использования:**
- Поврежденный период данных
- Обнаружены пропущенные сообщения
- Нужно обновить конкретный временной диапазон

```bash
python3 telegram_sync.py --person "Contact A" --since "2025-10-01"
```

**Пример вывода:**
```
🔄 Режим перезагрузки: перезапись с 2025-10-01T00:00:00
✅ Перезагружено 19 сообщений с 2025-10-01
```

**File structure:**
```markdown
# Contact A

## Telegram history with Contact A (handle: @contact_a)

[... messages before 2025-10-01 - preserved ...]

## Reloaded from 2025-10-01T00:00:00 on 2025-10-17T18:01:18

- 2025-10-16T04:53:05 ➡️: Recent message
- 2025-10-08T20:34:37 ➡️: Message from Oct 8
[... all messages from Oct 1 onwards ...]
```

---

## 🔒 Философия: Telegram = Личное пространство

**Принципы**:
- Telegram - это личное пространство, НЕ рабочий инструмент по умолчанию
- Синхронизировать ТОЛЬКО то, что явно помечено для работы
- По умолчанию: НЕ синхронизировать
- Явный контроль через теги

**Теги**:
- `#DO_SYNC` ✅ - Работа (инвесторы, партнеры, бизнес)
- `#DO_NOT_SYNC` 🚫 - Личное (друзья, семья)
- `#NOT_ON_TELEGRAM` 📵 - Человек не в Telegram
- `#NOT_CONNECTED` 🔌 - В Telegram, но не в ваших контактах

---

## 📁 Системные файлы

**⚠️ Соглашение об именовании**: 
- ВСЕ скрипты Telegram ДОЛЖНЫ начинаться с префикса `telegram_` для последовательности и легкой идентификации
- ВСЯ документация Telegram находится ТОЛЬКО в ЭТОМ файле - НЕ создавайте новые файлы документации
- Держите документацию компактной и сфокусированной - высокая плотность информации

```
Scripts/
├── telegram_sync.py                      # ⚡ Sync messages (FAST with user_id!) ← MAIN SCRIPT
├── telegram_find_handles.py              # 🔍 Find handles + user_ids (for new contacts)
├── telegram_add_user_ids_batch.py        # 🚀 Batch add user_ids to all contacts
├── telegram_reload_all.py                # 🔄 Batch reload all contacts (clean files)
├── telegram_contact_preview.py           # 👁️ Preview contact details
├── telegram_analyze_status.py            # 📊 Status analysis
└── telegram_common.py                    # 🔧 Shared utilities (parsing, credentials)

Context/
├── sync_list_telegram.txt             # ← MASTER LIST with handles, user_ids, tags
├── Telegram_Sync_Status.md            # Current contact status report
└── [This folder has detailed docs]

Contacts/
└── [Name]/
    └── [Name] telegram.md             # Generated message history (chronological)
```

---

## 🎨 Формат основного списка (sync_list_telegram.txt)

```
# Новый формат с user_id (БЫСТРО - синхронизация в 3 раза быстрее!):
Name -> TelegramHandle [ID:user_id] #TAG [DONE:timestamp]

# Примеры:
Contact A -> @contact_a [ID:12345678] #DO_SYNC [DONE:2025-10-18T00:25:00]
Contact B -> Contact B Display [ID:23456789] #DO_SYNC [DONE:2025-10-18T00:30:15]
Contact C -> @contact_c [ID:123456] #DO_NOT_SYNC
Contact D #NOT_ON_TELEGRAM
Contact E #NOT_CONNECTED

# Без user_id (медленнее, будет запрашивать добавление user_id):
Name -> TelegramHandle #TAG [DONE:timestamp]

# Разбор формата:
Name                    - Имя контакта
-> TelegramHandle       - @username или отображаемое имя
[ID:12345]             - Telegram user_id (ОБЯЗАТЕЛЬНО для быстрой синхронизации!)
#TAG                    - DO_SYNC | DO_NOT_SYNC | NOT_ON_TELEGRAM | NOT_CONNECTED
[DONE:2025-10-18T...]  - Временная метка последней синхронизации
```

## ⚡ Производительность: Почему важен user_id

**Без user_id**: 15-20 секунд на контакт (медленное сканирование iter_dialogs)
**С user_id**: 5-6 секунд на контакт (прямой поиск + ранний выход)

**Улучшение скорости**: **В 3 раза быстрее!**

Для 64 контактов:
- Без user_id: 15-20 минут
- С user_id: 6-7 минут ⚡

**Как добавить user_ids**:
```bash
# Пакетное добавление для всех контактов
python3 telegram_add_user_ids_batch.py

# Или вручную при добавлении нового контакта
python3 telegram_find_handles.py --person "Name" --search
# Увидеть user_id в результатах, затем:
python3 telegram_find_handles.py --person "Name" --save "@handle" --user-id 12345678
```

---

## 📊 Быстрая справка: Когда использовать какой скрипт

### Используйте `telegram_find_handles.py` когда:
1. ✅ Добавляете нового человека в список
2. ✅ Не знаете их Telegram handle
3. ✅ Нужно проверить/обновить существующий handle
4. ✅ Хотите получить user_id для контакта

### Используйте `telegram_sync.py` когда:
1. ✅ Handle + user_id уже известны (существуют в списке синхронизации)
2. ✅ Нужны регулярные обновления сообщений
3. ✅ Пакетная синхронизация всех контактов #DO_SYNC
4. ✅ Нужна скорость (user_id = быстро!)

### НЕ используйте `telegram_sync.py` когда:
1. ❌ У контакта нет handle (нет -> в списке синхронизации)
   → Сначала используйте `telegram_find_handles.py`
2. ❌ У контакта нет user_id (нет [ID:...])
   → Сначала добавьте user_id или запустите `telegram_add_user_ids_batch.py`
3. ❌ Контакт помечен #NOT_ON_TELEGRAM
   → Человек не использует Telegram
4. ❌ Контакт помечен #DO_NOT_SYNC (для режима --all)
   → Конфиденциальность соблюдена, синхронизация не будет выполнена

---

## 🚀 Распространенные сценарии

### Сценарий 1: Добавить новый бизнес-контакт

**Интерактивный рабочий процесс (рекомендуется):**
```bash
# 1. Поиск handle
python3 telegram_find_handles.py --person "New Contact" --search --query "Contact"
# Возвращает JSON с user_id:
# {
#   "user_id": 12345678,
#   "username": "@contact_handle",
#   ...
# }

# 2. AI/пользователь подтверждает, какой результат правильный, и отмечает user_id

# 3. Сохранить handle С user_id (ВАЖНО!)
python3 telegram_find_handles.py --person "New Contact" --save "@contact_handle" --user-id 12345678

# 4. Первая синхронизация (скачивает все сообщения - БЫСТРО с user_id!)
python3 telegram_sync.py --person "New Contact"
# Занимает 5-6 секунд вместо 15-20!
```

**Результат в списке синхронизации:**
```
New Contact -> @contact_handle [ID:12345678] #DO_SYNC [DONE:2025-10-18T...]
```

**⚠️ ВАЖНО**: Всегда сохраняйте user_id! Без него синхронизация не удастся или будет очень медленной.

---

### Сценарий 2: Еженедельное обновление всех контактов

```bash
python3 telegram_sync.py --all --report report.txt
```

**Что происходит:**
- Обрабатывает ТОЛЬКО контакты с тегом `#DO_SYNC`
- Каждый контакт: скачивает сообщения после их собственной временной метки `[DONE:...]`
- Пропускает `#DO_NOT_SYNC`, `#NOT_ON_TELEGRAM`, `#NOT_CONNECTED`
- Обновляет временные метки для всех синхронизированных контактов
- Генерирует отчет с результатами

---

### Сценарий 3: Перезагрузить поврежденный период

```bash
# If you noticed missing/corrupted messages from October
python3 telegram_sync.py --person "Contact A" --since "2025-10-01"
```

**Что происходит:**
- Сохраняет сообщения до 1 октября
- Удаляет сообщения с 1 октября и далее
- Перескачивает сообщения с 1 октября и далее
- Добавляет маркер перезагрузки
- Обновляет временную метку

---

### Сценарий 4: Пометить контакт как личный

```bash
# Редактировать sync_list_telegram.txt
# Изменить:  Contact F -> @contact_f #DO_SYNC
# На:       Contact F -> @contact_f #DO_NOT_SYNC

# Следующий запуск --all: пропустит этот контакт
```

---

## 📊 Текущая статистика

По состоянию на 2025-10-18:

- **Всего контактов в Contacts/**: 169
- **Все контакты отслеживаются в списке синхронизации**: 169 ✅
- **Контакты с #DO_SYNC**: 105
  - **С user_ids (БЫСТРО)**: 56 контактов ⚡
  - **Без user_ids**: 8 контактов (требуется повторное обнаружение handle)
  - **Без handles**: 41 контакт (требуется первоначальное обнаружение)
- **Контакты с #DO_NOT_SYNC**: 32
- **Контакты с #NOT_ON_TELEGRAM**: 21
- **Контакты с #NOT_CONNECTED**: 11

**Telegram файлы с сообщениями**: 56+ файлов
- Успешно синхронизировано 56 контактов с полной историей сообщений
- Всего сообщений: 1,400+ по всем контактам
- Выявлено 8 контактов с устаревшими handles (требуется повторное обнаружение)

---

## ⚡ Следующие шаги

### Для использования в продакшене:

1. ✅ **Система готова** - все контакты отслеживаются, дубликаты объединены
2. 📥 **Синхронизировать все контакты #DO_SYNC** - запустить режим `--all`
3. 🔍 **Просмотреть результаты** - проверить сгенерированные файлы telegram.md
4. 🏷️ **Настроить теги** - изменить любые неправильно классифицированные контакты
5. 🔄 **Еженедельные обновления** - запланировать запуск `--all`

---

## 💡 Профессиональные советы

1. **@username лучше всего**:
   - Постоянный идентификатор
   - Быстрое точное совпадение
   - Выживает при изменении отображаемого имени

2. **Отображаемое имя - запасной вариант**:
   - Работает, когда нет @username
   - Может сломаться, если человек изменит имя
   - Скрипт сохраняет оба, когда доступны

3. **Теги ручные**:
   - Просмотрите после первой пакетной синхронизации
   - Измените DO_SYNC → DO_NOT_SYNC для личных контактов
   - Система уважает ваш выбор конфиденциальности

4. **Инкрементальный по умолчанию**:
   - Быстрые ежедневные обновления
   - Скачивает только новые сообщения
   - Используйте --since только когда нужно (перезагрузка/исправление)

5. **Мониторьте report.txt**:
   - Показывает всю историю синхронизации
   - Помогает отладить проблемы
   - Отслеживает количество сообщений

---

## 🆘 Устранение неполадок

### Контакт не найден:

```bash
# Проверить, что в списке:
grep -i "name" sync_list_telegram.txt

# Проверить, что handle правильный:
python3 telegram_contact_preview.py --handle @username

# Повторно найти handle, если нужно:
python3 telegram_find_handles.py --person "Name" --search
```

### Слишком много контактов синхронизируется:

```bash
# Проверить теги:
grep "#DO_SYNC" sync_list_telegram.txt | wc -l

# Добавить теги DO_NOT_SYNC для личных контактов
```

### Нужно обновить личный контакт:

```bash
# Использовать --person (работает даже с тегом DO_NOT_SYNC):
python3 telegram_sync.py --person "Contact F"
```

### Сообщения кажутся неполными:

```bash
# Перезагрузить с конкретной даты:
python3 telegram_sync.py --person "Contact A" --since "2025-09-01"
```

---

## 📨 Синхронизация Inbox (AI Inbox)

**Скрипт**: `telegram_inbox_sync.py`  
**Назначение**: Синхронизация сообщений из группы Telegram "[AI_INBOX_NAME]" (личный inbox)  
**Выход**: `../Inbox/telegram ai inbox.md`

### Быстрое использование:

```bash
# Инкрементальная синхронизация (по умолчанию - рекомендуется)
python3 telegram_inbox_sync.py

# Перезагрузить все сообщения
python3 telegram_inbox_sync.py --reload

# Перезагрузить с даты/времени (поддерживается несколько форматов)
python3 telegram_inbox_sync.py --since "2025-10-21 14:30"
```

### Детали реализации:

**Обнаружение группы**:
- ID группы: `1234567890` (захардкожено в скрипте - личный inbox пользователя)
- Прямой поиск через `client.get_entity(GROUP_ID)` (быстро)
- Запасной вариант: если ID не работает, ищет по имени "[ai_inbox_name]"
- Поддерживает оба типа: Channel и Chat

**Режимы синхронизации**:
1. **Инкрементальный** (по умолчанию): Добавляет новые сообщения после последней временной метки синхронизации
2. **Перезагрузить все** (`--reload`): Перезаписывает файл всеми сообщениями
3. **Перезагрузить с даты/времени** (`--since`): Перезаписывает файл сообщениями с указанного времени

**Файлы состояния**:
- `../Inbox/.last_sync_timestamp` - Время последней синхронизации (формат ISO) для инкрементальной синхронизации

**Поддерживаемые форматы даты/времени**:
- Только дата: `2025-10-21` (по умолчанию 00:00:00)
- Дата + время (минуты): `2025-10-21 14:30`
- Дата + время (секунды): `2025-10-21 14:30:45`
- Формат ISO: `2025-10-21T14:30:00`

**Формат сообщения**:
```
**YYYY-MM-DDTHH:MM:SS** (Имя отправителя):
Содержимое сообщения

**YYYY-MM-DDTHH:MM:SS** (Имя отправителя):
🎤 [Голосовое сообщение]
📝 Расшифровка: [Автоматически расшифрованный текст из Telegram Premium]
```

**Расшифровка голосовых сообщений**:
- Автоматически запрашивает расшифровку для голосовых сообщений через `TranscribeAudioRequest`
- Требует подписку Telegram Premium
- Прогрессивная повторная попытка с задержками: 0с, 1с, 2с, 3с, 4с, 5с (ждет, пока Telegram сгенерирует расшифровку)
- Проверяет флаг `result.pending`, чтобы определить, все еще обрабатывается ли
- Расшифровка появляется в markdown выводе, когда готова
- Молча пропускает, если недоступна после всех попыток

**Случай использования**: Идеально для нескольких синхронизаций в день. Пользователь может запускать инкрементальную синхронизацию утром/днем/вечером для непрерывной обработки сообщений inbox.

**Учетные данные**: Использует те же учетные данные Telegram, что и другие скрипты, через `telegram_common.get_telegram_credentials()`

---

**Для более подробной информации**: См. другие файлы Telegram_*.md в этой папке

**Последнее обновление**: 2025-10-21 (Добавлена система синхронизации inbox с поддержкой точного --since по дате/времени)
